services:
  webhooks-inspector:
    image: postgres:17
    container_name: webhooks-inspector
    environment:
      - POSTGRES_USER=docker
      - POSTGRES_PASSWORD=docker
      - POSTGRES_DB=webhooks
    ports:
      - "5432:5432"
